'use strict';

import initTimedContent from './timed-content';

export default {
    /**
     * Initialises any timed-content blocks we find on a page...
     */
    'init': function() {
        initTimedContent();
    }
};